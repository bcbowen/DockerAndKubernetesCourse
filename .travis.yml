language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t bcbowen/docker_course -f Dockerfile.dev .

script:
  - docker run -e CI=true USERNAME/docker-react npm run test
